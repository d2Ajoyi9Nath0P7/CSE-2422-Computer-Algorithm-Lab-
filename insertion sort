#include<bits/stdc++.h>
using namespace std;

void insertion_sort(vector<int>&vec)
{
    int i,j,item;
    for(int i = 1 ; i < n ; i++)
    {
        item = vec[i];
        j = i - 1;
        while(j >= 0 && vec[j] > item)
        {
            vec[j+1] = vec[j];
            j--;
        }
        vec[j+1] = item;
    }
}
int main()
{
    int n;
    cin>>n;
    vector<int>vec(n);
    for(int i = 0 ; i < n ; i++)
    {
        cin>>vec[i];
    }
    insertion_sort(vec);
    for(int i = 0 ; i < n ; i++)
    {
        cout<<vec[i]<<" ";
    }
}
